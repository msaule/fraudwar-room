FROM python:3.11-slim

WORKDIR /app
COPY backend /app/backend
COPY data /app/data
RUN pip install --no-cache-dir -e /app/backend
WORKDIR /app/backend
CMD ["uvicorn", "fraudwar.main:app", "--host", "0.0.0.0", "--port", "8000"]

